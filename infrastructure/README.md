# infrastructure

Placeholder for deployment/container infrastructure definitions.

**Implementation scaffolding** — this directory is reserved by the approved
foundation plan; no infrastructure definitions exist yet. GPU workloads run on
RunPod behind the `@stratifit/compute` abstraction; no GPU CI is configured.
