export default function PublishingPage() {
  return (
    <main className="mx-auto max-w-5xl p-8">
      <h1 className="text-2xl font-bold">Publishing</h1>
      <p className="mt-2 text-gray-400">
        Publication records and target adapters are managed here; publishing remains a
        separate domain from production.
      </p>
      <div className="mt-6 rounded-lg border border-dashed border-gray-700 p-8 text-center text-gray-500">
        Placeholder — no publication records yet.
      </div>
    </main>
  );
}
